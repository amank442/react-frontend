apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-Ingress
  namespace: default
spec:
  rules:
  - host: foo.bar.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
